#!/usr/bin/env bash
# Wrapper so callers never need to know about the venv.
# Usage: ./content/cinegrade <command> [args]
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$HERE/.venv/bin/python" "$HERE/grade/cinegrade.py" "$@"
